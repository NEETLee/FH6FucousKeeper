# 更新日志

本项目所有重要变更都记录在此文件中。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.2] - 2026-06-30

围绕全自动刷图大循环的稳定性修复，重点解决「读数读错把流程卡住」和「技术点刷不满目标」两类问题。

### 修复
- **余额 / 技术点识别更准**：修复赛后偶尔把 CR 余额读成十位数巨大值、或把技术点少读一位的问题——这类误读会让程序误判「余额 / 技术点不足」从而跳过买车、抽奖、清车库，看上去像卡住。现在会过滤掉混进数字里的玩家昵称、把 CR 限制在游戏上限以内，并对低对比度数字做增强后多帧取样比对再采用。
- **刷技术点保证刷满目标**：修复跑完预定圈数后经常达不到目标值（例如停在 944 不到 999）的问题。跑圈改为闭环——每跑完一批就回读技术点，没到目标自动补跑，直到达标（并设有安全上限，避免误读跑飞）。
- **避免重开丢技术点**：跑到终点后先等结算界面把技术点入账，再重开下一圈，防止结算未完成就重开导致这一圈技术点丢失。

## [1.3.1] - 2026-06-29

这一版从「防暂停 + 自动跑图」迈向**全自动刷图挂机**：开一次就能自己循环刷钱、刷技术点、开抽奖、清车库，人可以离开。
（1.3.0 因发布包漏装模板图片无法使用，已由本版取代；下面是 1.3 的完整更新内容。）

### 新增
- **真正的后台运行，不抢占前台**：依托防暂停内核 + 直接对游戏窗口取画面、向游戏窗口送操作，整套自动刷图在后台进行——游戏窗口不需要置顶、不会被强制切到前台，你可以同时用电脑做别的事，鼠标键盘也不会被脚本霸占。这是相比常见前台截屏 / 模拟按键类脚本（需独占屏幕和输入、稍微动一下就会打断）的核心优势。
- **全自动刷图大循环**：一键完成「进赛事 → 跑圈拿技术点 → 读取余额 → 批量买车 → 超级抽奖开盲盒 → 清理车库」的完整流程，并自动循环。可设置每轮买几辆、刷多少抽奖；程序会按当前余额和目标技术点，自动算好每轮要买的数量和要跑的圈数。
- **自动进入指定赛事**：在配置里填入 EventLab 蓝图分享码，程序自动进入创意工坊、填码、确认，进入你要刷的那张图。
- **自动选车（认车名 + 性能值）**：只要在赛事配置里写下刷图用车的车名和性能评分（PI），程序就能自动翻到品牌、找到这辆车并选中；即使上一轮删车把当前车换掉了，下一轮也能稳定选回目标车。**不再依赖一换分辨率就失效的车辆截图**。
- **自动处理赛后点赞弹窗**：跑完出现「点赞 / 点踩」蓝图评价弹窗时自动关掉，不再卡住循环。
- **画面识别全面升级**：换用新的屏幕捕获 + 图像匹配 + 文字识别方案，换游戏分辨率 / 窗口大小也能稳定读出 CR、技术点和按钮位置。

### 修复
- 修正跑圈计数：按实际完成的圈数累计，而不是每轮只记 1 圈。
- 流程结束后自动关闭画面捕获，消除游戏窗口上残留的黄色捕获边框，方便判断何时可以自己接管操作。
- 修复发布包内模板图片缺失、导致自动刷图无法识别游戏画面的问题（模板图片此前被版本控制误忽略，没有打进发布包）。

### 变更
- 发布包体积显著减小（运行库经 UPX 压缩）。

### 致谢
- 本版本的自动刷图流程设计与界面模板图片，借鉴自开源项目 [**fh6auto（YOUSTHEONE/FH6Auto）**](https://github.com/YOUSTHEONE/FH6Auto)。其「循环跑图 / 批量买车 / 超级抽奖 / 大循环挂机」的模块化思路与蓝图分享码进图、目标车辆匹配等流程，给了本项目直接启发。在此向 fh6auto 作者致以诚挚谢意。

## [1.3.0] - 2026-06-29

- 1.3 的首个发布,但发布包误未打入模板图片导致自动刷图无法识别画面,已由 1.3.1 取代。完整更新内容见 1.3.1。

## [1.2.1] - 2026-06-09

### 修复
- 修复「清除日志」按钮点击无效的问题（原先在主窗口上查找日志控件，而它实际是日志页面板的子控件，导致找不到、清不掉；现改为直接通过保存的句柄清空）。

### 变更
- 更换为高清多尺寸应用图标（16~256 像素，多分辨率）。
- 新增发布流程规则文档。

## [1.2.0] - 2026-06-07

### 新增
- 新增繁体中文界面。

### 变更
- UI 改进与细节打磨。
- 移除旧的消息回放（msg_replay）模块。

### 修复
- 发布包补入 `profiles/` 目录。
- 修正发布工作流的 YAML 编码问题。

## [1.1.0] - 2026-06-06

### 新增
- 自动跑图（auto-race）系统。
- 启动时检查新版本。
- 运行期间防止系统休眠。

## [1.0.0] - 2026-06-05

### 新增
- 防暂停核心：DLL 注入 + 窗口子类化，拦截失焦相关消息，使游戏失去焦点时不暂停（Steam 版与 Microsoft Store 版通用）。
- Win32 多标签图形界面（状态 / 窗口列表 / 日志 / 设置）。
- 游戏静音开关。
- 中英文国际化与系统语言自动检测。
- 高 DPI 支持（2K / 4K 缩放）。
- 作者签名、GitHub 仓库链接、窗口模式使用提示。
- 基于 GitHub Actions 的自动构建与发布流水线。

[1.3.2]: https://github.com/NEETLee/FH6FucousKeeper/compare/v1.3.1...v1.3.2
[1.3.1]: https://github.com/NEETLee/FH6FucousKeeper/compare/v1.3.0...v1.3.1
[1.3.0]: https://github.com/NEETLee/FH6FucousKeeper/compare/v1.2.1...v1.3.0
[1.2.1]: https://github.com/NEETLee/FH6FucousKeeper/compare/v1.2.0...v1.2.1
[1.2.0]: https://github.com/NEETLee/FH6FucousKeeper/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/NEETLee/FH6FucousKeeper/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/NEETLee/FH6FucousKeeper/releases/tag/v1.0.0
